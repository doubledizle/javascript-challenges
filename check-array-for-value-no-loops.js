// Write a function that takes an array and searches each element for a value and returns true or false, but you can't use a loop

const check = (array, value) => array.includes(value) ? true : false
